import { createClient } from "@/lib/supabase/server";
import { TransactionForm } from "@/components/TransactionForm";

function money(value:number){return new Intl.NumberFormat("de-DE",{style:"currency",currency:"EUR"}).format(value)}
export default async function TransactionsPage(){const supabase=await createClient();const {data}=await supabase.from("transactions").select("id,description,amount,type,category,transaction_date").order("transaction_date",{ascending:false});return <><header className="topbar"><div className="page-title"><h1>Transactions</h1><p>Record and understand every movement of money.</p></div></header><section className="grid-2"><div className="panel"><h3>Add transaction</h3><TransactionForm/></div><div className="panel"><div className="panel-head"><h3>Your ledger</h3><span className="muted">{data?.length??0} records</span></div>{data?.length?<div className="table-wrap"><table><thead><tr><th>Description</th><th>Category</th><th>Date</th><th>Amount</th></tr></thead><tbody>{data.map(r=><tr key={r.id}><td>{r.description}</td><td>{r.category}</td><td>{new Date(r.transaction_date).toLocaleDateString("en-GB")}</td><td className={r.type==="income"?"amount-positive":"amount-negative"}>{r.type==="income"?"+":"-"}{money(Number(r.amount))}</td></tr>)}</tbody></table></div>:<div className="empty">No transactions yet.</div>}</div></section></>}
